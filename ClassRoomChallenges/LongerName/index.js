let myName = "Sean";
console.log(myName.length);

let fName = "Michelle";
console.log(fName.length);

const myNameLength = myName.length;
const friendNameLength = fName.length;

if (myNameLength > friendNameLength) {
    console.log(`${myName}'s name is longer than ${friendNameLength}`);
} else {
    console.log(`${fName}'s name is longer than ${myName}'s`)
}

console.log(myNameLength - friendNameLength);

(myNameLength > friendNameLength) {
    
    
    console.log(`${myName}'s name is longer than ${friendNameLength}'s by ${myNameLength - friendNameLength} letter`);
} else {
    console.log(`${fName}'s name is longer than ${myName}'s`)
}
